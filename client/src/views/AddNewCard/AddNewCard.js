import React from 'react';
import CardForm from "../../components/CardForm";

const AddNewCard = () => {
    return (
        <div>
            <CardForm />
        </div>
    );
};

export default AddNewCard;